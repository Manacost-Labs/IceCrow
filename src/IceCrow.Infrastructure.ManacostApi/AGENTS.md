# Manacost data boundary

This project is optional enrichment infrastructure. Use public HTTPS without a
shared secret, enforce response/page/string/count limits before retaining data,
validate cache hashes and identities, and keep the last-known-good snapshot on
network or validation failure. Never make tracking depend on API availability.
HTTP/cache/image types must not leak into `Hearthstone.Data` contracts.
